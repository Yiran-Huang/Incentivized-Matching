- `main0.py`: Simulation of the more vulnerable trust case (**Figure 6**). The running time may be long.
- `main1.py`: Simulation of the less vulnerable trust case (**Figure 5**). The running time may be long.

To view the figures, first run the scripts and wait for the outcomes to be automatically saved. Then enter the `plots` folder in the root directory and run `generate_figures`.

